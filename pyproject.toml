[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ccbox"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
ccbox = "ccbox.__main__:main"
